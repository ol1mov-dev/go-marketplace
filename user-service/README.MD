# User Service [ Сервис пользователей ]

`user-service` — микросервис для управления пользователями в системе marketplace.  
Сервис отвечает за регистрацию пользователей, хранение учетных данных и работу с ролями.

## 🚀 Основные возможности

- Создание пользователя
- Назначение роли пользователю
- Проверка уникальности email
- Хранение пользователей в PostgreSQL
- gRPC API

---

## 🛠 Технологии

- Go
- gRPC
- PostgreSQL
- sqlx
- sqlmock (тестирование)

---

## ⚙️ Конфигурация

Сервис использует PostgreSQL в качестве основной базы данных.

### 📦 PostgreSQL

| Параметр      | Значение       |
|---------------|----------------|
| Хост          | `localhost`    |
| Порт          | `5432`         |
| База данных   | `user_service` |
| Пользователь  | `postgres`     |
| Пароль        | `postgres`     |

Пример строки подключения:

```text
postgres://postgres:postgres@localhost:5432/user_service?sslmode=disable
